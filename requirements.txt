# FastAPI & ASGI Server
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database: SQLAlchemy + PostgreSQL
SQLAlchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Pydantic (for config & validation)
pydantic==2.7.1
pydantic-settings==2.2.1

# OpenAI LLM APIs (if using OpenAI or Azure OpenAI)
openai==1.33.0

# CORS for frontend/backend communication
fastapi[all]==0.111.0

# Redis (for caching or async task queues, optional)
redis==5.0.4

# Optional: background tasks and tools
httpx==0.27.0
tenacity==8.2.3

# Optional: Logging
loguru==0.7.2

# Optional: dotenv (for local env vars)
python-dotenv==1.0.1
anthropic
pytest
httpx
typing-extensions
tiktoken
pymupdf
